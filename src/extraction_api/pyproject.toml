[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "extraction_api"
version = "0.1.0"
description = "Assignment extraction package"
requires-python = ">=3.10"

dependencies = [
]

[tool.setuptools]
packages = ["extraction_api"]