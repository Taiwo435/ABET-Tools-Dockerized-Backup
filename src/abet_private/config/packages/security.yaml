# Check refference at https://symfony.com/doc/7.4/reference/configuration/security.html
security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'bcrypt'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: 'email'

    firewalls:
        dev:
            # Ensure dev tools and static assets are always allowed
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            form_login:
                login_path: app_login # redirect to this path if user not logged in
                check_path: app_login
                default_target_path: app_homepage # once user logs in, go here
                enable_csrf: true
            logout:
                path: app_logout
                # where to redirect after logout
                target: app_login
                # target: app_any_route

            # Activate different ways to authenticate:
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Note: Only the *first* matching rule is applied
    access_control:
        # Public, unauthenticated entry points
        - { path: ^/login, roles: PUBLIC_ACCESS }
        - { path: ^/logout, roles: PUBLIC_ACCESS }

        # Native Symfony routes migrated as part of #132
        - { path: ^/admin/templates, roles: ROLE_ADMIN }
        - { path: ^/admin/users, roles: ROLE_ADMIN }
        - { path: ^/admin, roles: ROLE_ADMIN }
        - { path: ^/home, roles: ROLE_USER }
        - { path: ^/profile, roles: ROLE_USER }
        - { path: '^/tool/assignmentsgrades*', roles: ROLE_ASSIGNMENTS_GRADES }

        # Native Symfony routes migrated as part of #132 (batch 1: account/*)
        - { path: ^/account/profile, roles: ROLE_USER }
        - { path: ^/account/settings, roles: ROLE_USER }

    # NOTE (#132): Legacy-bridged routes (src/public/*.php, dispatched via
    # LegacyBridge.php) are NOT yet covered here — they still rely on
    # require_login()/require_role() checks inside each individual file
    # rather than the Symfony firewall. Extending access_control to cover
    # those paths (~28 files) is remaining work under this same ticket.

# Issue #132 Activated central framework firewalls to enforce automatic endpoint security

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: bcrypt
                cost: 4 # Lowest possible value for bcrypt

