# The Application

While ambiguous, in this project, `app` refers to the Apache<->PHP docker container that handles user HTTP requests using the PHP interpreter. 

## Package Management

It would be ideal if we used php `composer` to manage php packages, similar to how `pip3` manages python packages in `requirements.txt`
